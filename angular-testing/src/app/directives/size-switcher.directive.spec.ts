import { SizeSwitcherDirective } from './size-switcher.directive';

describe('SizeSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
