import { DateStatePipe } from './date-state.pipe';

describe('DateStatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateStatePipe();
    expect(pipe).toBeTruthy();
  });
});
